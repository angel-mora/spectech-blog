<p id="notice" class="m-0 p-0"><%= notice %></p>
<div class="row categories-container m-0 p-0">
  <% @categories.each_with_index do |cat,i| %>
    <div class="col-3 m-0 p-0">
      <%= top_img(cat,i) %>
      <div class="half">
        <%= link_to cat.title %>
        <%= link_to 'Read more', cat, class:'' %>
      </div>
      <%= bottom_img(cat,i) %>
    </div>
  <% end %>
</div>
